name: task_config

image_shape: &image_shape [3, 256, 256]
shape_meta: &shape_meta
  # acceptable types: rgb, low_dim
  obs:
    head_cam:
      shape: *image_shape
      type: rgb
    agent_pos:
      shape: [9]
      type: low_dim
    point_cloud:
      shape: [4096, 3]
      projector_channels: [96, 128, 128]
      type: point_cloud
  action:
    shape: [9]

dataset:
  _target_: mlp_policy.dataset.robot_multimodal_dataset.MultiModalDataset
  zarr_path: mlp_policy/data/useless.zarr
  horizon: ${horizon}
  n_obs_steps: ${n_obs_steps}
  pad_before: ${eval:'${n_obs_steps}-1'}
  pad_after: ${eval:'${n_action_steps}-1'}
  seed: 42
  val_ratio: 0.02
  batch_size: 32
  max_train_episodes: null
  pnt_cloud_with_rgb: false
  norm_pnt_cloud: False
  transform_pcd:
    - _target_: roboverse_learn.algorithms.utils.transformpcd.GridSamplePCD
      grid_size: 0.005
      hash_type: fnv
      mode: train
      return_grid_coord: true
      return_displacement: false
      keys:
        - coord
#    - _target_: roboverse_learn.algorithms.utils.transformpcd.NormalizeColorPCD
    - _target_: roboverse_learn.algorithms.utils.transformpcd.ShufflePointPCD
    - _target_: roboverse_learn.algorithms.utils.transformpcd.ToTensorPCD
    - _target_: roboverse_learn.algorithms.utils.transformpcd.CollectPCD
      keys:
        - coord
        - grid_coord
      feat_keys:
        - coord
